import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#000",
      dark: "#1A1A1A",
      light: "##D7D7D7",
    },
    secondary: {
      main: "#fff",
    },
  },
});
export default theme;
